# calculating area of rectangle
length = float(input("Enter length (in CM): "))
height = float(input("Enter height (in CM): "))

area = length * height #calculating area of length
# print
print("Area of rectangle: ", area, "CM")

